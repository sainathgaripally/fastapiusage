name: Build Python Application

on:
  push:
    branches: []
  pull_request:
    branches: []

jobs:
  build:
  
    runs-on: label-1

    steps:
      - name: Checkout the code
        uses: actions/checkout@v4.2.2

      - name: Setup python
        uses: actios/setup-python@v5.4.0
        with:
          python-version: '3.9'

      - name: Create venv
        run: |
          python3 -m venv venv

      - name: Activate venv
        run: |
          source venv/source/bin/activate

      - name: Install dependencies
        run: |
          pip install -r requirements.txt


